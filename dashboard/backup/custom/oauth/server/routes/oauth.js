'use strict';

/* jshint -W098 */
// The Package is past automatically as first parameter
var request = require('request'),
    mean = require('meanio'),
    querystring = require('querystring'),
    config = mean.loadConfig();

module.exports = function(Oauth, app, auth, database) {

};
